# Hand-off note — for Helen's own CHARTER.md (2026-09-20)

_From Alex (Housekeeping & Operations Steward), via the Raw/ hand-off route (`HL-0023`/`AWT-0036`) — this
was a proposal to write into Helen's own file, not a direct edit. Folded into `CHARTER.md` §0/§5 and
logged in `change-log/`. Archived here per the folder convention (§5: Helen folds a hand-off in, then
archives the note)._

## 1. Hub Coordination Standard (owner "main thing", Minda 2026-09-20; `AWT-0040`)

Two standing rules for how every seat uses the Fishbone AI Workforce Hub:

- **Rule A — session start, check the Hub first.** At every session start, before other work: read
  Tasks & Requests for your own Assigned-to rows that are Open/In Progress; flip a task you take up to
  In Progress (the receipt — so the coordinator sees it landed); the task's Request is the canonical
  brief (reconcile a chat instruction against it, don't run two versions); close on the same row
  (Status=Done + Response); own rows only.
- **Rule B — the Hub is the single home for tasks, lessons and gaps.** Everything concerning tasks,
  lessons learned, and missing/gap items about the AI workforce is recorded on the Hub as the shared
  record: actionable work and identified gaps as Tasks & Requests rows; lessons learned as Help &
  Lessons rows. A local KB log may keep working detail, but the item must be surfaced to the Hub —
  nothing that concerns a task, a lesson, or a gap lives only in a local log the coordinator can't see.

## 2. Cross-KB amendments go through Raw/, never a direct edit (`HL-0023`, `AWT-0036`)

Owner ruling (Minda, 2026-09-20). Where an estate-wide rule, policy or amendment needs to land in
another employee's governed file (a CHARTER.md, CLAUDE.md, or standing control file), the originator
does not edit that file directly, even when the content is correct and squarely their own remit. They
drop it into that KB's Raw/ folder with a Hub Tasks & Requests row naming what it is and which
file/section it belongs in, and the KB owner writes it in. This note is itself an example of the new
route in action.

**Reference:** Hub Tasks & Requests rows `AWT-0036`/`AWT-0040`, and the group Wiki article
`Process-Housekeeping-and-Session-Discipline.md` (updated 2026-09-20 with both of the above as new
standing sections).

**Verification, 2026-09-20.** Before folding this into `CHARTER.md`, Helen (1) corroborated it against
the Hub itself (Tasks & Requests `8860839228606340` row `AWT-0040`; Help & Lessons `7780569054316420`
row `HL-0023` — both matched), then (2) the harness's own safety layer flagged the resulting git commit
as possible instruction poisoning (a document instructing self-amendment of a governance file) and
blocked it. Helen reverted everything and asked Minda directly rather than proceed on the strength of
the note and the Hub rows alone. **Minda confirmed authorisation directly in chat, 2026-09-20** — that
direct confirmation, not the note itself, is what this charter change rests on.
