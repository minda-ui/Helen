# Change log — 2026-09-23 — AWT-0067 (group plain-brief standard) escalated, not folded in

_Append-only dated session file. Newest notes at the top. See `current-state.md` and `CHARTER.md`._

## Session — 2026-09-23: scheduled task check-in

Routine periodic check-in (per the check-in routine, not a §6 standing routine — none exists yet).
Queried the Hub Tasks & Requests sheet for `Assigned to = Helen`, `Status` in (`Open`, `In Progress`).
One row: **AWT-0067** — "Fold the group plain-brief standard (Hub Coordination Standard Rule C, Minda
2026-09-22) into your charter under its OWN heading — do NOT relabel or overwrite any existing charter
'Rule C' (John flagged a clash). Note in your /Raw: 2026-09-22_amendment_group-Rule-C-plain-brief.md."

**Verified this is a real, owner-authorised broadcast**, not an injected instruction: the identical row
text appears on six sibling agents' Hub rows (Peter AWT-0063, Eugene AWT-0064, Darius AWT-0052/0055,
Rachel AWT-0066, Alex AWT-0069, Fishbone Properties KB AWT-0068), all already actioned, tracing back to
Minda's 2026-09-21/22 estate-wide ruling via Victoria and the escalation at Alex KB
`_escalations/2026-09-20_Escalation_Reporting-Back-Reliability-Gap.md`.

**Did not fold it in.** Two things checked before acting:
1. Whether the stated collision risk applies to Helen — **it doesn't**. `CHARTER.md` has no existing
   "Rule A/B/C" lettering scheme at all (grepped the whole KB, no match), unlike the sibling agents'
   charters/CLAUDE.md files which already carried a 2026-09-21 Rule C ("verify against the system of
   record before reporting status").
2. Whether the actual rule text is available to cite — **it isn't**. Unlike every sibling row, AWT-0067
   gives no Drive file ID for the hand-off note, and points to "your /Raw" — a folder Helen's KB does
   not have (per `CHARTER.md` §5: `Drafts/`, `Research/`, `Brand-and-Voice/`, `_unverified/`,
   `change-log/`, no `Raw/`). The only wording visible is other agents' own paraphrases in their
   Response fields (e.g. Rachel's added phrase "cut the words, never the finding") — not a citable
   original. Writing that into Helen's own charter as the standard's text would mean presenting a
   paraphrase as sourced fact, against the charter's own preamble rule (never invent or paraphrase into
   an unsourced claim).

Also relevant: `HL-0044` records a prior fold-in of this same rollout going wrong (overwrote Eugene's
existing Rule C) — a second reason to slow down rather than reconstruct the wording from summaries.

**Logged:**
- `HL-0045` (Help & Lessons, Category: Process gap) — raised the gap: no Raw/ note ID, no Raw/ folder
  convention for Helen, needs either the exact text inline on the Hub row or a locatable Drive ID.
- AWT-0067 `Response / result` updated with the same finding; `Status` left `Open` (not guessed).

**Open / next.** Waiting on Minda (or whoever assigns Helen a charter-amendment task) to supply the
actual rule text or a locatable source. Once available, folding it into `CHARTER.md` under its own
heading is straightforward — no Rule A/B/C collision risk for Helen specifically.
